# Failure Atlas

Every real-world pattern HubbleOps has met and how it was handled. Fed by the
[real-repo loop](../prompts/cross-cutting/real-repo-loop.md) from the Phase 2 gate onward, and by
[Phase 10](../prompts/phases/10-pilot-hardening.md) pilot hardening.

Rules:

- No customer identifiers. Anonymize the pattern; keep the shape.
- One row per pattern, added the moment it is found — not at the end of a phase.
- A pattern that is genuinely undecidable statically gets `PRESERVED UNKNOWN` in *Disposition* and a
  precise closing instruction. That is a correct outcome, not a gap.
- No rule tuned to a single repo's names. If it can't generalize, it's a fixture, not a rule.

| ID | Pattern | Language | Found by | Should be caught by | Phase | Fixture | Disposition |
|---|---|---|---|---|---|---|---|
| FA-001 | Module-level version constant interpolated into REST endpoints is recalled but not resolved to an effective version. | Python | Phase 2 real-repo loop | Structure constant/data-flow observer | 3 | `python_rest_query_wrapper` | NEW_PATTERN; preserve UNKNOWN until the constant is proven to reach the request sink. |
| FA-002 | Multiline or formatted query assembled from literal fragments and holes reaches an HTTP payload. | Python | Phase 2 real-repo loop | Structure query-skeleton observer | 3 | `python_rest_query_wrapper` | PRESERVED UNKNOWN; close with an observed request bound to the source hash when a hole remains. |
| FA-003 | Imported wrapper carries an inline query through a local function into a provider HTTP sink. | Python | Phase 2 real-repo loop | Import graph and structure wrapper walk | 3 | `python_imported_wrapper` | PRESERVED UNKNOWN; resolve the wrapper chain, leaving contract validation to Phase 6. |
| FA-004 | Ordinary prose containing “automatically generated” is misclassified as generated output. | Markdown | Phase 2 real-repo loop | Source Closure generated-marker classifier | 3 prerequisite | `generated_marker_scope` | NEW_PATTERN; prose remains INSIDE unless a generator marker occurs in an artifact-header context. |
| FA-005 | Explicit “autogenerated / do not edit” file header is missed when its wording or directory name is not in the narrow marker list. | TypeScript | Phase 2 real-repo loop | Source Closure generated-marker classifier | 3 prerequisite | `generated_marker_scope` | NEW_PATTERN; classify as GENERATED only with artifact-local evidence. |
| FA-006 | Exported TypeScript version constant flows through an import into a REST endpoint and computed proto lookup. | TypeScript | Phase 2 real-repo loop | Import graph and structure constant/data-flow observer | 3 | `typescript_version_symbol` | NEW_PATTERN; require both sinks to resolve to the same literal version. |
| FA-007 | A Python namespace-shaped string in Markdown or TypeScript data is incorrectly emitted as an AFFECTED Python call version. | Markdown / TypeScript | Phase 2 real-repo loop | Language-scoped text and structure observers | 3 | `language_scoped_carrier` | NEW_PATTERN; retain the surface reference but do not assign an effective version without a language-appropriate sink. |
| FA-008 | A resolved lock entry does not close the matching manifest dependency UNKNOWN. | TypeScript dependency metadata | Phase 2 real-repo loop | Dependency resolver cross-evidence linking | 3 | `npm_lock_cross_evidence` | NEW_PATTERN; close only the dependency-state uncertainty with the content-hashed lock evidence. |
| FA-009 | Resolved third-party SDK version has no evidence-backed provider API compatibility mapping. | TypeScript dependency metadata | Phase 2 real-repo loop | Provider compatibility resolver | 3 / 6 | `npm_lock_cross_evidence` | PRESERVED UNKNOWN; close with a content-hashed compatibility source or a human target decision bound to the dependency hash. |
| FA-010 | Parent-relative TypeScript import retains `..` and misses a local exported version constant before a computed carrier. | TypeScript | Phase 3 real-repo loop | Import graph path normalization | 3 | `typescript_parent_import` | NEW_PATTERN → CLOSED_WITH_EVIDENCE; normalize parent segments before matching closure-relative paths, then retain the imported literal through local aliases. |
| FA-011 | A runtime that reserves a large virtual address space cannot start under an address-space rlimit sized as a memory bound, so hook capture of that language is impossible while every other language still passes. | JavaScript (V8); any runtime with a reserved heap cage | Phase 4 implementation | Sandbox resource limits | 4 | `probe.cjs` | CLOSED_WITH_EVIDENCE; bound resident memory with `RLIMIT_DATA` and keep `RLIMIT_AS` only as a separate, larger address-space bound. Both bounds are proved against every capture runtime. |
| FA-012 | A per-language hook loader silently does not run for some invocation forms of its own language, so an unobserved execution is indistinguishable from a suite that never calls the provider. | PHP (`auto_prepend_file` skipped for `php -r`); any loader a runtime may bypass | Phase 4 implementation | Dynamic capture loader attestation | 4 | `probe.php` | CLOSED_WITH_EVIDENCE; the hook writes a per-run nonce and capture emits `HOOK_NOT_INSTALLED` instead of `UNKNOWN_DYNAMIC`, so "the sensor never loaded" and "no provider call happened" carry different closing instructions. |
| FA-013 | A deep host state directory makes a bind-mounted file unreadable inside the container (`EIO`), so the workload silently loses the capture certificate, every intercepted request fails its TLS handshake, and the run looks like a suite that made no provider calls. | Any language, proxy mode | Phase 4 implementation | Proxy interception accounting | 4 | `probe.py` | CLOSED_WITH_EVIDENCE; a failed handshake is counted from the proxy's own log and reported as `PROXY_INTERCEPTION_FAILED`, which suppresses the `UNKNOWN_DYNAMIC` "no events" claim. Prefer a short state directory; the workload's `EIO` is preserved in the run's stderr artifact. |
| FA-014 | Git refuses to remove its own worktree metadata because the directory carries the read-only attribute, so a capture leaves state inside a repository it promised not to modify. | Any language | Phase 4 real-repo loop | Detached worktree lifecycle | 4 | `test_a_worktree_whose_removal_is_refused_still_restores_the_repository` | CLOSED_WITH_EVIDENCE; removal retries, then clears the read-only attribute and prunes, and the lifecycle still fails closed if the metadata does not return to its prior state. The attestation records `recovered`. Observed on both pinned repositories, so the recovery path is load-bearing, not incidental. |
| FA-015 | A test command that fetches dependencies at run time makes the workload's package manager attempt egress, which the deny-all proxy correctly observes and denies; the request is not provider traffic, so it becomes `UNKNOWN_WIRE_SIGNATURE` and one UNKNOWN candidate per unclassifiable request. | Any language, proxy mode | Phase 4 real-repo loop | Wire signature classification | 4 / 5 | none — behaviour is correct | PRESERVED UNKNOWN; the reason names the exact target so a human can dismiss it. Dismissing non-provider hosts automatically is **rejected for now**: an incomplete pack host list would then hide real usage, which is the failure the laws exist to prevent. Revisit only if a real suite's unclassifiable egress swamps its ledger. |
| FA-016 | Every source path is passed on one command line, so beyond roughly 500-800 files the operating system refuses to start the analyzer and the failure is reported as a missing tool that is in fact installed and working. | Any language | Post-Phase-4 stress measurement | `graph/imports.py` argument batching | 4 | `test_query_invocations_track_batches_not_file_count` | CLOSED_WITH_EVIDENCE; paths are batched under a 24,000 character budget against the 32,767 character Windows `CreateProcess` limit, and a refused start with the executable present is now `TOOLING_FAILED` naming the argument length, never `TOOLING_MISSING`. Measured: 800 files failed before, 4,000 files pass after. |
| FA-017 | An enumerator and a searcher walk the same tree with independently maintained exclusion rules, so a directory pruned from the closure is still reached by the search and the run fails closed on a path the closure never enumerated. | Any language | Post-Phase-4 hardening | Source Closure exclusion surface | 4 | `test_the_searcher_never_reaches_a_directory_the_closure_did_not_enumerate` | CLOSED_WITH_EVIDENCE; `SourceClosure.search_exclusions()` is the single source of truth and ripgrep derives its globs from it. Two hand-maintained lists of the same invariant is a defect with a delay, not a style choice. |
| FA-018 | Wrapper-walk lookups scan every match in the repository and filter by path, so analysis cost grows with the product of candidates and repository size rather than with the file being analysed. | Any language | Post-Phase-4 profiling | Import graph path indexes | 4 | `test_the_graph_answers_path_lookups_without_scanning_the_repository` | CLOSED_WITH_EVIDENCE; the graph builds path and id indexes once, and `SourceRange.contains` fell out of the top twelve profile entries. Graph build 5.64 s to 3.39 s on 60 files, and the saving grows with repository size because the complexity class changed. |
| FA-019 | A response reader keeps a removed field by assembling its name from parts (`"campaigns." + "legacy"`), so no single literal carries the removed name and a name-matching consumer check sees nothing. Every other check is honestly clean, so the migration reaches `VERIFIED_FOR_SCOPE` and breaks at runtime. | Python; any language with string concatenation or interpolation | Phase 5 red-team | Response-consumer check | 5 | `tests/adversarial/split_literal_response_read` | CLOSED_WITH_EVIDENCE; the check now reads the graph's `concatenation` and `format` nodes as well as its atoms, reassembles a name whose parts are all literals, and emits a named UNKNOWN when an assembly mixes in something it cannot resolve. **This was a P0**: one corruption defeated the single stage guarding an entire attack class. |
| FA-020 | A check reports PASS because it had nothing to look at. Zero requests reach the oracle, every falsifier is skipped for want of its failure class, or a base UNKNOWN's candidate id simply vanishes — each returns "no failures found" and the conjunct reads as proven. | Any language | Phase 5 red-team | Contract oracle, falsifiers, UNKNOWN conservation | 5 | `test_an_oracle_that_saw_nothing_while_subjects_changed_is_unresolved`, `test_a_falsifier_set_that_never_ran_is_unresolved_not_a_pass`, `test_an_unknown_that_vanishes_entirely_is_a_conservation_failure` | CLOSED_WITH_EVIDENCE; a check with nothing to look at now reports it as unresolved, which caps the verdict at UNKNOWN. An absence of evidence is never evidence of absence, and a conjunct of the frozen verdict rule that cannot fail is a conjunct that is not there. |
| FA-021 | A binary diff has a changed path but no textual hunk, while a top-level edit has a hunk but no parsed definition; both disappear from containment or blast-radius accounting when those stages iterate only textual hunks or definitions. | Any language / binary | Phase 5 gate audit | Git diff containment and base/candidate graph radius | 5 | `test_a_binary_change_is_never_absent_from_containment`, `test_a_top_level_source_change_enters_unknown_blast_without_coverage`, `test_a_definition_deleted_from_the_base_graph_enters_unknown_blast` | CLOSED_WITH_EVIDENCE; binary paths materialize synthetic hunks, every changed source module enters reach even without a definition, and deleted base definitions are preserved in the radius. |
| FA-022 | Phase 5 accepts a Phase 4 capture as arbitrary JSON, then looks for a legacy `request` object even though the frozen event schema emits `request_text`; oracle, shape, audit and falsifier stages silently lose dynamic evidence. | Any captured client | Phase 5 gate audit | Verification input boundary and every dynamic verification stage | 5 | `test_a_schema_invalid_capture_is_refused`, `test_a_phase4_capture_reaches_the_oracle_as_a_request_mapping`, `test_an_old_version_dynamic_capture_fails_the_migration_audit` | CLOSED_WITH_EVIDENCE; captures are bounded and validated by Phase 4's schema, Search text and JSON bodies are reconstructed conservatively, versions enter request-shape identity, and dynamic residue reaches audit and falsifiers. An opaque body stays unresolved. |
| FA-023 | Changing the base SHA, version pair, obligations, decisions, captures or live oracle authority can change a verification verdict while the candidate-derived ProofScope hash stays fixed. | Any language | Phase 5 gate audit | Frozen ProofScope and ContractOracle context binding | 5 | `test_every_verdict_affecting_input_moves_the_verification_manifest_hash`, `test_a_capture_without_an_execution_manifest_is_refused`, `test_an_oracle_result_is_rejected_if_its_bound_context_moves` | CLOSED_WITH_EVIDENCE; ProofScope now binds a canonical, versioned verification-input manifest and a non-secret oracle context identity. Obligations and decisions must match the base scan and run, captures must match their source SHA, provider, schema, payload and execution manifest, and a moving oracle context fails closed. |
| FA-024 | A resolution walk exhausts its explicit work budget but emits no evidence, or the remaining request is relabelled `CONTRACT_VALIDATION_DEFERRED`, so a truncated analysis reads like a fully resolved request awaiting only provider validation. | Any structurally analysed language | Repository intelligence implementation | Resolution budget accounting and request resolution | 6 | `test_an_exhausted_resolution_budget_yields_unknown_and_never_a_resolved_value` | CLOSED_WITH_EVIDENCE; every exhausted walk emits `RESOLUTION_BUDGET_EXHAUSTED` evidence, stays UNKNOWN with a precise closing instruction, and never shares the label used by a completely resolved request skeleton. |
| FA-025 | A compositional resolution cache stores an entry-specific hop prefix inside a reusable summary, then prepends the new caller prefix on lookup, so provenance contains the original call site in the middle of a different audit trail. | Any structurally analysed language | Repository intelligence implementation | Resolution summary cache | 6 | `test_a_reused_summary_carries_the_calling_wrapper_chain_not_the_cached_one` | CLOSED_WITH_EVIDENCE; cached summaries strip their entry prefix and are rebased onto the requesting wrapper chain. Path-dependent and self-embedded summaries are not cached, so optimization cannot rewrite provenance. |
| FA-026 | A valid repository checks out at its original path but the isolated capture worktree adds enough path prefix to cross Windows' legacy path limit, so capture fails before the sandbox starts. | Any language on Windows | Phase 7 real-repo loop, `woocommerce/google-listings-and-ads` at `b43b322` | Detached worktree Git invocation | 4 | `test_detached_worktree_enables_long_paths_for_every_git_operation` | CLOSED_WITH_EVIDENCE; every bounded Git operation in the detached-worktree lifecycle now enables Git's scoped `core.longpaths=true` setting. The setting is command-local, transcripted, and does not mutate either repository or global Git configuration. |
| FA-027 | A generated verification workflow hardcodes one provider's secret names inside the generic Proof layer, so adding a provider requires violating the provider boundary or shipping an Action whose live oracle cannot authenticate. | Any provider | Phase 7 full regression gate | Provider-owned Action environment bindings | 7 | `test_provider_owned_secrets_reach_the_workflow_without_entering_generic_code`, `test_generic_layers_contain_no_provider_name` | CLOSED_WITH_EVIDENCE; an optional pack capability supplies inert environment-to-secret-name bindings through `app/`. Generic workflow code validates and sorts the identifiers, never sees secret values, and remains provider-neutral. Recorded as P-019. |
| FA-028 | A new fail-closed rule in the verifier reclassifies an acceptance that existing test fixtures already produced, so those fixtures quietly stop meaning what their names say while every assertion still passes. An oracle fixture called "accepting" became an unavailable one, and the suite stayed green. | Any language | P-020 implementation | Oracle authority attribution | 5 | `test_an_acceptance_that_names_no_authority_is_refused`, `test_the_review_reports_the_weakest_authority_that_accepted_anything` | CLOSED_WITH_EVIDENCE; the fixtures now state an authority, a separate fixture covers the unattributed case, and the review's authority is asserted directly. A green suite after a fail-closed change is evidence about the fixtures as well as the code: tightening what counts as proof can silently empty a test of its subject. |
| FA-029 | A source-bound human decision is validated against the base scan when read, then revalidated against the candidate scan when applied, so the two necessarily different run identities make the entire decision channel unreachable. | Any language | Phase 5 definition-of-done audit | Decision application across verification runs | 5 | `test_a_recorded_human_decision_closes_an_unknown_through_verify`, `test_a_decision_recorded_against_another_run_never_reaches_the_candidate` | CLOSED_WITH_EVIDENCE; decision application receives the explicit run and ProofScope in which the decision was recorded, while its source location is independently revalidated against the candidate ledger. Cross-run rules now have cross-run integration fixtures. |
| FA-030 | Evidence record identity includes run id, ProofScope and repository SHA, so the same observation rescanned in a candidate run always appears to be new evidence and the UNKNOWN-conservation violation branch cannot fire. | Any language | Phase 5 definition-of-done audit | UNKNOWN conservation evidence comparison | 5 | `test_an_unknown_closed_without_evidence_still_fails_across_two_scan_runs`, `test_a_repeated_observation_under_a_new_run_is_not_new_evidence`, `test_a_changed_observation_under_a_new_run_is_new_evidence` | CLOSED_WITH_EVIDENCE; `observation_identity` removes record-envelope identity while retaining source hash and value, so a repeated observation is not new and a changed observation is. New evidence remains candidate-attached rather than anything new elsewhere in the run. |
| FA-031 | A human decision remains attached after the source blob it judged changes, allowing an approval for one request to close a different request at the same path and line. | Any language | Phase 7 source-binding review | Candidate-side decision provenance validation | 7 | `test_a_source_bound_decision_cannot_survive_candidate_blob_drift`, `test_a_decision_dies_when_the_source_it_was_made_about_changes` | CLOSED_WITH_EVIDENCE; application requires exactly one candidate evidence record matching the recorded path, line, claim type and blob hash. A moved or ambiguous subject is refused rather than inherited. |
| FA-032 | A catalog shape validator distinguishes objects from scalars but accepts any scalar value for any protobuf scalar or enum, so a string boolean or integer resource name can receive `VALID` catalog authority. | Any protobuf JSON request | P-021 final review | Target-catalog scalar shape validation | 5 | `test_catalog_mutate_validation_fails_closed` | CLOSED_WITH_EVIDENCE; primitive booleans, strings, floating-point values, signed/unsigned integer widths, wrapper types and enums now enforce their JSON scalar kinds and integer bounds. Null remains accepted as protobuf JSON's unset value; semantic formats such as timestamps and resource names remain explicitly outside catalog authority. |
| FA-033 | A generic layer hardcodes the scope of one provider's CATALOG authority, so the Receipt and the frozen schema deny a capability the same build proves; here both stated that catalog authority proves nothing about "any mutate shape" while P-021 granted catalog authority for exactly that. | Any provider pack | P-021 tree audit | Receipt authority rendering and the generic-surface boundary | 7 | `test_the_receipt_states_the_packs_own_catalog_scope_and_asserts_none_of_its_own`, `test_no_generic_surface_states_what_a_providers_catalog_authority_covers` | CLOSED_WITH_EVIDENCE (P-022); `proof/receipt.py` quotes the distinct stated `reason` of every accepted CATALOG check instead of asserting a scope, a CATALOG acceptance that states no scope renders as proving nothing, and the boundary test fails any generic surface that names a provider capability. The stale sentence could not be caught before because no test pinned it. |
| FA-034 | A provider identifier that is also the application's own domain vocabulary is recalled once per lexical occurrence, so imports, docblocks, type hints, stylesheet selectors, help-centre URLs and UI action slugs each raise their own UNKNOWN while the genuine call sites are a small minority of the matches. The recall record carries no syntactic role, so nothing downstream can distinguish a comment from a call. | PHP / JavaScript; any language | Phase 7 real-repo loop, `woocommerce/google-listings-and-ads` at `b43b322` | Text observer syntactic-context evidence | 6 | `tests/fixtures/phase6/adjudication` | PARTLY CLOSED_WITH_EVIDENCE (P-023); the structural layer now adjudicates every recall match in a file it parsed, emitting the containing node kind and the symbol binding as `PROVEN` evidence that outranks the recall claim by per-claim precedence. Measured on the same commit: **128 of the 496 resolved — 109 to NOT_AFFECTED_WITH_EVIDENCE (all `comment` nodes, independently audited, zero false safes) and 19 to AFFECTED**. A file the structural layer did not parse produces no adjudication and the recall UNKNOWN survives, so absence of adjudication is never a disposition. A line carrying the name both in code and in a comment is deliberately **not** adjudicated. The remainder needs Q22 string reachability and non-code carriers. A name-based exclusion list stays **rejected**: it would hide real usage. |
| FA-035 | One token listed in two surface categories — an identifier and a package name — raises two independent candidates for a single observation, each with its own closing instruction, so the UNKNOWN count overstates the number of undecided facts. | Any language | Phase 7 real-repo loop, `woocommerce/google-listings-and-ads` at `b43b322` | Surface spec category overlap and ledger claim keying | 6 | none yet | PRESERVED UNKNOWN; 194 (path, line, subject) triples were claimed twice — 83.6% of all `package_reference` candidates and 25.8% of every UNKNOWN in the run. Neither claim may simply be dropped: this needs a recorded decision about claim identity, not new evidence. |
| FA-036 | A call site whose version is fixed by a versioned import in the same file stays UNKNOWN, because the import's version literal and the construction site are separate candidates and nothing binds the symbol to its import. | PHP; any language with an import table | Phase 7 real-repo loop, `woocommerce/google-listings-and-ads` at `b43b322` | Import-table symbol binding in the graph | 6 | `tests/fixtures/phase6/adjudication` | CLOSED_WITH_EVIDENCE (P-023); a per-file import/alias table now binds an identifier to the module it was imported from, and a binding whose namespace carries a version resolves the site to AFFECTED at that version. The construction site and its import reach the same version, which is asserted directly. A token that is only a *substring* of a bound alias is deliberately left UNKNOWN, because substring-of-alias reasoning is unsound. First-party bindings are handled by FA-039, never by calling them safe. |
| FA-039 | A repository defines its own class with the same name as a provider SDK class, deliberately overriding it. Binding proves the symbol is first-party, which is positive evidence that the token is not the provider's — and is **not** evidence that the site is safe, because the wrapper may still reach the provider. | PHP; any language permitting a shadowing definition | Phase 7 real-repo loop, `woocommerce/google-listings-and-ads` at `b43b322` | Import-table symbol binding plus wrapper following | 6 | `tests/fixtures/phase6/adjudication` | PRESERVED UNKNOWN, with a closing instruction that names the first-party definition to follow. 69 sites on the measured repository. The owner ruled on 2026-09-11 that a first-party binding is never a safety verdict: follow the implementation and ask whether it reaches the provider, and keep UNKNOWN while that walk is inconclusive. Resolving these to NOT_AFFECTED on the strength of the binding alone is **rejected** — it is the exact false-safe this pattern invites. |
| FA-037 | Source that closes its language tag and resumes inside markup — `?>` followed by HTML carrying interpolated blocks — produces ERROR nodes in the structural grammar, so the whole file is UNSCANNED and its text matches can never be resolved structurally. | PHP templates and view files | Phase 7 real-repo loop, `woocommerce/google-listings-and-ads` at `b43b322` | Structural parser language coverage | 6 | none yet | FILE_UNSCANNED, correct and fail-closed; 13 files, 7 of them view templates, one of which carried 14 unresolved text matches. Closing it needs a template-aware parse of the embedded code regions. Relaxing the ERROR-node check is **rejected**: a partially parsed file would silently under-report. |
| FA-038 | Query-language field tokens appear in front-end code that reads an API *response* and are recalled as request-language anchors, so a response consumer is reported as an unresolved request. | JavaScript; any front end consuming a provider-shaped payload | Phase 7 real-repo loop, `woocommerce/google-listings-and-ads` at `b43b322` | Request versus response claim classification | 6 | none yet | PRESERVED UNKNOWN; 10 of 19 anchors were response reads whose stated closing instruction — extract the request skeleton — cannot be followed at those sites. A claim whose own closing instruction is inapplicable is mislabelled, not merely uncertain, and these sites belong to the response-consumer class the verdict already checks. |
| FA-040 | The recall channel and the structural channel each test their own hand-written copy of the same resource enumeration, so a resource missing from the list is missing from both and the usage disappears with **no candidate at all** — not as UNKNOWN. The completeness math assumes observers fail independently; two copies of one list make them fail together. | TypeScript / JavaScript / PHP; any pack with a request language | Phase 7 real-repo loop, `dubinc/dub` at `b8866f4` | Catalog-derived request-language classification | 6 | `tests/fixtures/coverage/gaql_resource_beyond_anchor_list` | CLOSED_WITH_EVIDENCE (P-026); the resource set is now **derived from the provider catalog**, which already carried the missing resource with PROVEN provenance, and a grammar-level shape matches every query regardless of resource. A resource the catalog knows resolves; one it does not know becomes `UNRECOGNISED_RESOURCE` UNKNOWN inside provider context. Adding the missing name to the list was **rejected**: it repairs one example and leaves the class, and nothing would notice the next one. |
| FA-041 | A wire-level protobuf identifier that appears verbatim in JSON, TypeScript, Go and Java is declared as a language-specific SDK carrier, so a version literally present on the line reads as "carries no version" in every language but the one declared. | TypeScript / Go; any language carrying a proto type URL | Phase 7 real-repo loop, `dubinc/dub` at `b8866f4` | Version carrier scope | 6 | `tests/fixtures/coverage/proto_namespace_outside_python` | CLOSED_WITH_EVIDENCE (P-026); `VersionCarrier.scope ∈ {wire, sdk}` and the loader **refuses** a `wire` carrier that restricts languages, so the mistake is unrepresentable rather than merely corrected. A wire carrier is language-independent by construction, per §6.6 and P-008. |
| FA-042 | A provider REST call is issued through a local wrapper whose parameters are a destructured options object and whose call sites use explicit type arguments, so the wrapper is not a definition, the call is not a call, the request path binds to nothing, and the endpoint literal is never reached. | TypeScript; any language with named/keyword arguments | Phase 7 real-repo loop, `dubinc/dub` at `b8866f4` | Definition coverage and named-parameter binding in the wrapper walk | 6 | `tests/fixtures/coverage/rest_path_behind_local_wrapper` | CLOSED_WITH_EVIDENCE; async arrows, generics, class methods and generic call syntax are now definition/call forms, `Definition.bindings` maps each destructured name to its slot, and the walk narrows the caller's object-literal argument to the named property. The endpoint resolves to its literal; a path built at runtime stays `UNKNOWN_QUERY_HOLE`. Narrowing matters for precision as well as recall: without it every destructured name resolved to the whole object and unrelated sibling values entered the version evidence. |
| FA-043 | A path-aliased in-repo import (`@/lib/...` via `tsconfig.json` `paths`) resolves to nothing, so first-party modules are reported as defined outside the repository and the file importing the provider carries no provider context of its own. | TypeScript / JavaScript | Phase 7 real-repo loop, `dubinc/dub` at `b8866f4` | Language-aware module resolution | 6 | `tests/fixtures/coverage/path_alias_import` | CLOSED_WITH_EVIDENCE; `graph/imports.py` reads `tsconfig`/`jsconfig` `paths` and `baseUrl`, and provider context propagates one hop across a resolved import so an importing file's contract surface is no longer invisible. On the measured repository this class was **62 of 111 UNKNOWNs (56%)**. |
| FA-044 | A distinct API adjacent to the one under migration — reachable from the same integration, carrying its own version lattice — produces no record at all, so "not part of this contract" and "never seen" are indistinguishable. | Any language | Phase 7 real-repo loop, `dubinc/dub` at `b8866f4` | Adjacent-contract declaration | 6 | `tests/fixtures/coverage/adjacent_contract_host` | CLOSED_WITH_EVIDENCE (P-026); `SurfaceSpec.adjacent_contracts` yields `EXCLUDED_WITH_EVIDENCE` naming the neighbouring contract and why it is not the one under migration. Silence is not exclusion. |
| FA-045 | Contract surface that is neither a version nor a request — auth scopes, protocol headers, resource-name templates, error-shape reads — has no claim type, so it produces no candidate even inside a file already known to carry provider context. | Any language | Phase 7 real-repo loop, `dubinc/dub` at `b8866f4` | Contract-surface residue sweep | 6 | `tests/fixtures/coverage/contract_surface_residue` | PRESERVED UNKNOWN (P-026); pack-declared contract-surface *shapes* raise `CONTRACT_SURFACE_UNRESOLVED` inside provider context. Emitting these repository-wide is **rejected** — it would make every SQL `FROM` and every kebab-case string a provider candidate, trading a silent miss for destroyed precision. The gate is provider context, not a name list. |
| FA-046 | A repository that checks out correctly still exceeds Windows' legacy 260-character path limit when read, so files the closure enumerated cannot be opened and become UNSCANNED. Worse, the enumerator and ripgrep disagree about whether a deeper path exists at all, and the scan stops fail-closed on a path the closure never enumerated. | Any language on Windows | Phase 7 real-repo loop, `dubinc/dub` at `b8866f4` (7 unread files) and `googleads/google-ads-python` at `f068d59` (scan aborted at a 273-character path) | Source Closure enumeration and file access | 6 | `test_a_path_beyond_the_legacy_windows_limit_is_still_read` | CLOSED_WITH_EVIDENCE; both the closure walk and its reads go through an extended-length path. Fixing only the read is not enough — ripgrep handles long paths, so an enumerator that does not turns FA-017's invariant into an abort. Fail-closed was correct throughout: the files were UNSCANNED or the scan stopped, never silently skipped. |
| FA-048 | A package-relative import (`from . import x`, `from .. import y`) produces a module string of only dots, so module resolution builds an empty path and raises `ValueError` from inside the structure observer's thread. The scan dies with a traceback instead of failing closed with a typed reason — an L9 violation, and the one failure mode the laws exist to prevent. | Python; any language with relative import syntax | Phase 7 real-repo loop, held-out `googleads/google-ads-python` at `f068d59` | Import graph module resolution | 6 | `test_a_relative_import_never_crashes_module_resolution` | CLOSED_WITH_EVIDENCE; empty segments are dropped and an unresolvable specifier returns `None`, which the caller already treats as "not in this repository". This was **masked by FA-046**: the scan previously aborted at the enumeration/searcher disagreement before reaching the graph, so fixing the shallower bug is what exposed this one. A held-out repository found it; the design corpus never would have, because no fixture used a relative import. |
| FA-047 | A file that merely *imports* provider code, or that carries the provider's package name only as a path segment, is treated as provider context, so an unrelated query language in that file (a warehouse/analytics SQL dialect) is claimed as an unresolved provider request. | TypeScript; any repository with more than one query language | Phase 7 real-repo loop, `dubinc/dub` at `b8866f4` | Provider-context strength for the weakest signal | 6 | `tests/fixtures/coverage/foreign_query_language_nearby` | PARTLY CLOSED_WITH_EVIDENCE; the *unknown-resource* branch — the weakest signal in the system, a resource the provider's own catalog does not know — now requires **direct** provider context (host, package manifest, config key, or version carrier in the same file) and does not inherit it across imports. Measured on the same commit: **4 false alarms → 2**. The residual 2 are FA-035: `google-ads` is both an identifier and a package name, so it matches a path segment in `@/lib/integrations/google-ads/...` and grants that file direct context it has not earned. Closing those needs FA-035's recorded decision about claim identity, not a new rule. Catalog-*known* resources stay ungated deliberately: a real query over a real resource must never depend on what else is in the file. |
| FA-049 | A provider catalog compiled from two public sources treats the *absence* of one source as a *conflict* between them, so every field only one source lists is declared undecidable. Every metric, every segment and ordinary attributes like the customer id were "disputed" when nothing disagreed; the oracle refused `SELECT customer.id FROM customer`, 1,086 of 1,238 "changed" facts between two adjacent versions had byte-identical attributes, and the migration plan told the user to "resolve what replaces `metrics.clicks`". | Any provider pack with more than one catalog source | Owner's review of a real-repo migration plan, 2026-09-12 | Catalog reconciliation in the pack's change compiler | 6 | `tests/unit/test_catalog_reconciliation.py`, `tests/property/test_contract_diff_invariants.py` | CLOSED_WITH_EVIDENCE; one complete source resolves a field at DOCUMENTED confidence, a corroborating source raises it to PROVEN, and only two *present* sources that disagree make it UNKNOWN_PROVIDER_CONTRACT. Unresolved v25 fields 1,407 → 140 (all genuine type disagreements); adjacent CHANGED/UNKNOWN facts 1,238 → 27; a property test forbids any CHANGED fact whose before and after are equal. Google's own per-version field service is the right primary source and is raised as P-031. |
| FA-050 | The first report counts version-carrying *lines*, not migration *findings*, so one fact ("this repository is on V23") prints as 229 AFFECTED entries while the report says nothing about the effective version, the SDK floor, the sunset date, or which used fields the target removes. An AI reviewer's unit is the finding; the map's unit was the line, which is why "AI found 4, we found 1" compared different things. | Any language | Owner's review, 2026-09-12, on a real PHP repository | Exposure Map rendering against the target contract | 6 | `tests/unit/test_exposure.py` | CLOSED_WITH_EVIDENCE; `hops scan`/`hops exposure` take `--target`, the map gains MIGRATION FINDINGS (effective version per repository, SDK pin against the target's documented floor with `floor unknown` when the pack cannot compare, sunset dates, required changes grouped by finding with site and file counts), and AFFECTED groups version literals by carrier: `V23 · 229 sites · 37 files`. The ledger and ProofScope stay target-free (Q26). Every candidate is still rendered or counted; the grouping test asserts grouped plus ungrouped equals the AFFECTED count. |
| FA-051 | An impact report lists the paths of *carried* UNKNOWN/UNSUPPORTED/UNSCANNED obligations as "affected paths", so images, JSON test mocks and markdown appear as files the migration touches. Measured: 474 affected paths, 301 of them non-source. | Any language | Owner's review, 2026-09-12 | Impact and migrate rendering | 6 | `tests/unit/test_impact.py` | CLOSED_WITH_EVIDENCE; affected paths are the paths of DETERMINISTIC and HUMAN obligations only, findings are grouped by change id with site and file counts, carried obligations render in their own section grouped by closing instruction, and an obligation class the renderer does not know fails closed instead of vanishing. |
| FA-052 | The verifier drives one test runner (pytest) and picks a single runner per repository, so a PHP repository, or a mixed PHP and JavaScript one, can never reach VERIFIED_FOR_SCOPE: its frozen suite is `NO_FROZEN_TESTS` or only the JavaScript half runs. | PHP; any repository declaring more than one runner | Owner's review, 2026-09-12, on a real WooCommerce plugin | Frozen-suite runner plurality (P-028) | 6 | `tests/unit/test_phpunit_runner.py` | CLOSED_WITH_EVIDENCE; a `phpunit` layout runs the repository's own `vendor/bin/phpunit` with JUnit and clover output, every declared runner runs in a stable order and merges into one SuiteRun (worst outcome names the runner; a declared-but-missing runner sinks the whole run, so JS tests can never stand in for PHP tests), and coverage is credited only from a clover report that actually names executed files. PHPUnit's behaviour without a coverage driver is unverified on this machine and fails closed to unresolved. |
| FA-053 | Four noise mechanisms inflate UNKNOWN past what is undecided: one token in two surface dictionaries raises two candidates per line (FA-035); provider names inside documentation, stylesheets and lock files are treated like code; an identifier adjudicated as code and bound to a versionless symbol stays UNKNOWN although every version literal in its own file agrees; and a bare-word error-shape pattern matches ordinary identifiers like `errorCode`. | PHP / JavaScript; any language | Owner's review, 2026-09-12, on a real WooCommerce plugin and on `dubinc/dub` | Claim identity (P-024), closure roles, file-level version binding, qualified contract-surface shapes | 6 | `tests/unit/test_unknown_mechanisms.py`, `tests/property/test_adjudication_invariants.py`, `tests/fixtures/real_repo/glna_unknowns_before.json` | CLOSED_WITH_EVIDENCE; one observation is one candidate carrying every claim that matched (194 fewer on the measured repository), DOCUMENTATION/STYLESHEET/lock matches resolve NOT_AFFECTED_WITH_EVIDENCE naming the role (Q28), file-level binding fires only on a structurally adjudicated code site (absence of adjudication is still never a disposition), and `ads_failure_detail` matches only provider-shaped reads (29 → 2). Measured: UNKNOWN 672 → 358 with 0 unexplained and every AFFECTED candidate preserved; the first-party wrapper sites (FA-039) stay UNKNOWN by design. |
| FA-054 | `.tsx` files are counted as structurally supported and never parsed: the extension maps to the TypeScript grammar, which cannot parse JSX, so every recall match in a React file goes unadjudicated while coverage reports it supported. | TypeScript/React | Tier 3a real-repo loop on `dubinc/dub`, 2026-09-12 | Structure observer language mapping | 6 | `tests/fixtures/coverage/tsx_import` | CLOSED_WITH_EVIDENCE; `.tsx` is its own language with its own rule bundle, coverage reports a `tsx` row, and a React file the grammar still cannot parse becomes FILE_UNSCANNED rather than silently supported (one such file on the measured repository). |
| FA-055 | A provider name inside an import specifier that resolves to the repository's own module (`@/lib/integrations/google-ads/api`) is recalled as a provider reference and stays UNKNOWN, once per import line. | Any language with path imports | Tier 3a real-repo loop on `dubinc/dub` | Structural import adjudication | 6 | `tests/unit/test_unknown_mechanisms.py` | CLOSED_WITH_EVIDENCE; an import node whose specifier resolves in-repo is a first-party path segment, NOT_AFFECTED_WITH_EVIDENCE naming the resolved file; the module's own sites carry any provider call it performs. 24 sites on the measured repository. |
| FA-056 | Provider-shaped strings (`customers/…`, `conversionActions/…`, `SELECT … FROM tinybird`) in files whose only provider link is a first-party import are counted as contract surfaces because context reached the file through the import graph. | Any language | Tier 3a real-repo loop on `dubinc/dub` | Text observer file context | 6 | `tests/unit/test_unknown_mechanisms.py` | CLOSED_WITH_EVIDENCE; every text observation records `file_context` (direct, imported, none) and a gated surface without direct context in its own file resolves NOT_AFFECTED_WITH_EVIDENCE; candidates are still raised, never dropped. 17 sites on the measured repository. |
| FA-057 | Credential keys listed as config keys are reported as runtime-chosen versions; a version key matched as a substring of a longer identifier raises a second candidate; a constant definition `GOOGLE_ADS_API_VERSION = "v22"` is reported as an undecidable read instead of the version site it is. | JavaScript/TypeScript; any language | Tier 3a real-repo loop on `dubinc/dub` | Pack surface data, structural environment-read adjudication, config-literal carrier | 6 | `tests/unit/test_unknown_mechanisms.py` | CLOSED_WITH_EVIDENCE; config keys are the version-selecting keys only (§3.1), credentials are identifiers, environment reads are adjudicated structurally (configuration, never a version, unless the key selects a version), key patterns match on identifier boundaries, `.env*` files carry the CONFIG role, and a literal assignment to a version key is an AFFECTED site the migration edits. |
| FA-058 | Generic HTTP sinks (`fetch`, `post`, `request`) in files with no provider link at all, on wrapper chains with none, are reported as unresolved provider requests because the sink name alone matched. | Any language | Tier 3a real-repo loop on `dubinc/dub` (Shopify test helpers) and `google-listings-and-ads` (Merchant API client) | Resolver over skeleton fragments, file context and chain files | 6 | `tests/unit/test_unknown_mechanisms.py` | CLOSED_WITH_EVIDENCE; a sink whose fragments name no provider host, carrier or request language, in a file with no direct or imported provider link, on a chain whose files have none, is NOT_AFFECTED_WITH_EVIDENCE listing what was checked; any provider link anywhere on the chain keeps it UNKNOWN. |
| FA-059 | A fully resolved request skeleton is left `CONTRACT_VALIDATION_DEFERRED` because nothing judged it during the scan, and the map cannot say whether the query is valid. | Any request language | Tier 3a on both ground-truth repositories | Offline contract oracle at scan time; QUERIES block on the map | 6 | `tests/unit/test_exposure.py`, `tests/unit/test_unknown_mechanisms.py` | CLOSED_WITH_EVIDENCE; every resolved skeleton is judged against every lattice version with the offline catalog (AFFECTED with the accepting versions, or UNKNOWN naming the rejecting field), the map and `hops scan --target` render one verdict per request site against the target, and a query the catalog cannot decide says why (`conversion_action.status has conflicting provider sources` on the measured repository). |
| FA-060 | A recall match on a line the structural layer already resolved to a version or a request skeleton is counted a second time as its own UNKNOWN. | Any language | Tier 3a real-repo loop on `dubinc/dub` | Resolver over structural lines | 6 | `tests/unit/test_unknown_mechanisms.py` | CLOSED_WITH_EVIDENCE; the text match is explained by the structural evidence id on the same line and the structural candidate carries the site. |
| FA-061 | A precise indexer run on the customer tree **writes into it** (`scip-typescript --infer-tsconfig` creates `tsconfig.json` at the project root) and, with no `package.json` in reach, embeds the host's absolute path in every symbol string, so two machines index the same commit to different symbols. | TypeScript / JavaScript; any indexer that reads project metadata | Tier 3b spike on `google-listings-and-ads` `b43b322` and the design fixture | Precise-index staging (`graph/indexers.py`) | 6 | `tests/unit/test_indexers.py`, `tests/unit/test_precise_graph.py` | CLOSED_WITH_EVIDENCE; the indexer only ever runs on a staged copy with a synthesized root manifest, the output lives outside the copy, the reader never exposes the index's `project_root`, and a test snapshots the repository before and after the scan. |
| FA-062 | Every workspace `tsconfig.json` `extends` a base that only `node_modules` provides (`tsconfig/nextjs.json`, `@tsconfig/…`), so without the customer's dependencies the compiler-grade indexer indexes nothing for the project that matters; on Windows the workspace mode never starts because the drive colon is parsed as a project separator. | TypeScript monorepos | Tier 3b spike on `dubinc/dub` `b8866f4` (140 of 4,340 documents in workspace mode; `projects ["C"]` on Windows) | Precise-index staging | 6 | `tests/unit/test_indexers.py` (`package_style_extends_is_dropped…`) | CLOSED_WITH_EVIDENCE; the staged tsconfig is derived from the repository's own with unresolvable `extends` dropped and `paths`, `baseUrl` and `include` kept, every tsconfig directory is a project, and workspace flags are never passed. Measured on the same commit: 4,342 documents, 2,346 of 2,348 TypeScript and 1,991 of 1,991 TSX files precise. |
| FA-063 | The Python precise indexer does not start on Windows (`new RegExp(path.sep)` throws at import) and exhausts node's default heap on a generated client (`google-ads-python`: signal 6 at 2.27 GB after 118 s); with a 12 GB heap it completes in 1,733 s at 10.1 GB RSS. | Python | Tier 3b spike, WSL Ubuntu 24.04 | Precise-index runner platform and resource gates | 6 | `tests/unit/test_python_indexer.py` | CLOSED_WITH_EVIDENCE; the Python runner refuses Windows with the reason as `TOOLING_MISSING`, so the map says `recall-only: scip-python does not start on Windows …` instead of a traceback, and sets the heap itself on Linux. A generated client is the worst case; nothing pretends about the cost. |
| FA-065 | The compiler-grade indexer resolves `@types` packages and bare imports by walking **up from the staged copy**, out of the temporary directory and through the host's home, so a `node_modules/@types` two levels above `%TEMP%` injected a declaration that turned an UNKNOWN into AFFECTED with an identical ProofScope hash — two hosts, two verdicts, one proof. | TypeScript / JavaScript; any indexer with ancestor-walking resolution | Tier 3b red team, 2026-09-13 (`a18_ambient`) | Precise-index staging and the first-party symbol filter | 6 | `tests/unit/test_indexers.py` (`FORCED`), `tests/unit/test_precise_graph.py` (`external_symbol_occurrences_are_dropped…`) | CLOSED_WITH_EVIDENCE; the staged tsconfig forces `typeRoots: []` and `types: []`, and every occurrence whose symbol is not `local` and not owned by one of the repository's own manifests (or the synthesized one) is dropped before the graph sees the index, so the index is a function of the tree and the pinned indexer alone. The package branch of identifier adjudication went with it: the dependency observer owns packages and their versions. |
| FA-066 | A call dispatched through an interface or base class carries the declaration's symbol, so symbol-bound callers dropped the implementation's true caller, the second version literal vanished from the ledger with `unexplained 0`, and an ambiguous UNKNOWN closed to a single AFFECTED by deleting evidence. | TypeScript; any language with interfaces or inheritance | Tier 3b red team, 2026-09-13 (`a11_version_drop`) | Symbol-bound caller rule | 6 | `tests/fixtures/precise/ts_dispatch`, `tests/unit/test_precise_graph.py` (`a_call_through_an_interface…`, `…keeps_both_versions_visible`) | CLOSED_WITH_EVIDENCE; a name-and-arity caller is removed only when its symbol belongs to **another definition in the graph**; a declaration, local or unreadable symbol keeps the caller, and each caller hop says `by symbol` or nothing, so provenance is per caller, never per definition. Both versions stay visible on the fixture. |
| FA-067 | A call through a renamed import (`import { deliver as post }`) was rejected by the name guard on the symbol lookup, so the one construct symbol binding exists for stayed name-bound, and a same-named unrelated function elsewhere was counted as the caller instead. | TypeScript / JavaScript / Python | Tier 3b red team, 2026-09-13 (`a02_alias`) | Call-symbol lookup | 6 | `tests/unit/test_precise_graph.py` (`an_aliased_import_is_found_by_symbol…`) | CLOSED_WITH_EVIDENCE; the lookup is positional — the occurrence must start exactly at the callee's name and span exactly its length — so a renamed import resolves to its definition and any column drift still degrades to name binding. |
| FA-068 | Held-out transfer of the precise layer, run once and not tuned: `google/ads-api-report-fetcher` `2419122` (64 ts, 30 js, 56 py; a GAQL-heavy Google-owned tool). With the indexer: typescript precise 62 of 63, javascript 2 of 7 (the rest are Apps Script bundles the compiler cannot parse), python recall-only on this host; 2 tsconfigs rewritten without their `extends`, 2,437 external symbol occurrences dropped. Candidates 5,380 · AFFECTED 5 · NOT_AFFECTED 454 · EXCLUDED 4,323 · UNSUPPORTED 90 · UNKNOWN 501 · unexplained 0 — identical with and without the indexer, and `git status` clean afterwards. | TypeScript / JavaScript / Python | Tier 3b held-out run, 2026-09-13 | Precise-index layer end to end | 6 | none (held-out by definition) | RECORDED; the layer changed no disposition on a repository it never saw, which is the transfer claim this tier makes: precision refines provenance and never moves a verdict without evidence. The 501 UNKNOWNs are the next real-repo-loop input, not this tier's. |
| FA-064 | The JSON stream ripgrep emits is split with `str.splitlines()`, which also breaks on U+0085, U+2028, U+2029, vertical tab and form feed inside the matched source line, so a file carrying one of those characters (a minified Apps Script bundle) turns one JSON record into two and the text observer dies with `JSONDecodeError` — a traceback where a typed result was owed. | Any language; any file with a non-LF line separator | Tier 3b held-out transfer on `google/ads-api-report-fetcher` `2419122` | Text observer stream decoding | 6 | `tests/unit/test_text.py` (`…unicode_line_separator…`) | CLOSED_WITH_EVIDENCE; every line-delimited JSON stream (ripgrep matches, capture events) is split on `\n` only. A held-out repository found it in its first scan; no design fixture contained the character. |
